import React from 'react';
import MainNavigation from './main-navigation';

const AppHeader = () => (
  <header>
    <h1>Shopping List</h1>
    <MainNavigation />
  </header>
);

export default AppHeader;
